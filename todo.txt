Open questions

Q1. Is the front speaker actually at ~2-3° or is that a systematic method bias?
    Tuned AuxIVA now estimates 2° (was 4°). Still all methods share geometry/signal.

Q2. Do room acoustics transfer between example_mixture.wav and mixture.wav?
    Plausible, unconfirmed. SepFormer-WHAMR is consistent across both files (MOS ~2.6),
    and tuned AuxIVA params were found on example_mixture — need to validate on mixture.wav.

Immediate next

- Evaluate the first Spatial EM / GMM prototype on example_mixture.wav
- If it is promising enough, turn its soft masks into MVDR next

Completed today

✓ AuxIVA tuned: n_fft=2048, hop=512, n_iter=90 (was hop=1024, iter=30)
  Result: cross-corr sum ≈ 0.060 — less than half of ILRMA's 0.132. Big improvement.
  DoA estimates: 2°, 90°, 180°, 270° (front sharpened from 4° → 2°)
✓ Example demo now shows default vs tuned AuxIVA as separate variants
  Default shown by default; tuned hidden in its own subsection
✓ Mixture demo now shows default vs example-tuned AuxIVA as separate variants
  Default shown by default; tuned hidden in its own subsection
✓ ILRMA tuning finished
  Best trial #74: n_fft=2048, hop_size=512, n_iter=110, n_components=2, score=1.2559
✓ ILRMA default vs tuned variants added for example_mixture.wav and mixture.wav
  Default remains visible in demo; tuned ILRMA sits in its own hidden subsection
✓ First Spatial EM / GMM prototype implemented on example_mixture.wav
  Uses low-frequency TDoA bins for EM/GMM, then soft spatial masks for 4-channel reconstruction
  Current note: learned GMM templates collapse front/back, so mask construction falls back to SRP-seeded angles
✓ DUET on mixture.wav — finds 90°, 119°, 184°, 270°; misses front speaker entirely
  (LF-RR pair has poor angular resolution near 0° — known DUET limitation)
✓ DL on mixture.wav — SepFormer-WHAMR: MOS 2.61, cross-corr 0.235 (vs 0.099 on example)
✓ All outputs added to demo (player.html)

Next up — hackathon priority

1. Evaluate and tighten the Spatial EM / GMM prototype on example_mixture.wav
   - Listen to the current outputs and compare against DUET / AuxIVA / ILRMA
   - Improve the front/back split so the method does not need SRP-template fallback
   - Decide whether it is demo-worthy as its own separator

2. If the EM / GMM masks look promising: convert them into MVDR masks
   - EM-mask MVDR is probably the fastest high-upside hybrid method to demo
   - Keeps the story simple: probabilistic spatial clustering -> soft masks -> beamforming

3. Only after that: decide whether ILRMA-guided MVDR still adds something
   - Useful fallback hybrid if EM/GMM underperforms

Lower priority / optional

4. Diarization-guided MVDR (analysis/dl_separation/diarization_mvdr.py)
   - Ready to run, needs HuggingFace token:
       a. Accept https://huggingface.co/pyannote/speaker-diarization-3.1
       b. Accept https://huggingface.co/pyannote/segmentation-3.0
       c. Token at https://huggingface.co/settings/tokens
       d. export HF_TOKEN=hf_...  (add to ~/.zshrc)

5. Fine-tune SepFormer 2-src → 4-src (analysis/dl_separation/hpc_finetune_sepformer4.py)
   - Script ready. Needs: WSJ0-4Mix (LDC) or Libri4Mix (free), pip install speechbrain
   - sbatch hpc_finetune_sepformer4.py --train-dir ... --val-dir ...
   - ~12-24h on 1x A100
   - SepFormer-WHAMR already beats ILRMA cross-corr at 2 speakers (0.099 vs 0.132)

6. Neural ILRMA
   - Interesting research direction, but probably too large for the current hackathon loop

Working assumptions (confirmed)

- Mic geometry (D, L) fixed hardware — transfers across recordings
- AuxIVA and ILRMA are geometry-free; geometry only matters for post-hoc DoA labelling
- Speaker positions are scene-specific — re-estimate per file
- Tuned AuxIVA (hop=512, iter=90) outperforms default ILRMA on cross-correlation
- SepFormer-WHAMR (reverberant training) is the best off-the-shelf DL; beats ILRMA on MOS
- The clips are already fairly clean; separation / detangling matters more than denoising

Open assumptions (unconfirmed)

- Front speaker at ~2° vs method bias: unknown
- Room acoustics transferring between files: plausible, unconfirmed
- Whether EM / GMM masks beat DUET and approach AuxIVA quality on example_mixture: unknown
